doctype html
html
    head
        title Node server :)
        style 
            include style.css
    body
        div(class="table-title")

            table(class="table-fill")
                thead
                    tr
                        th(class="text-left")
                            | Identifiant
                        th(class="text-left")
                            | Ville
                tbody(class="table-hover")
                    each n in towns
                       tr
                        td(class="text-left")
                            | #{n.id}
                                
                        td(class="text-left")
                            | #{n.name}
                            //-    ((click)='()')